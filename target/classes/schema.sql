CREATE TABLE TRAINER (TRAINER_NAME VARCHAR(25) PRIMARY KEY,
                      LEVEL INT);

/*
TRAINER_ID INT PRIMARY KEY,
 */

CREATE TABLE POKEMON (ID INT PRIMARY KEY,
                    NAME VARCHAR(25),
                    TYPE VARCHAR(25),
                    TRAINER_NAME VARCHAR(25),
                    FOREIGN KEY(TRAINER_NAME) REFERENCES TRAINER(TRAINER_NAME));

